#!/bin/bash
trap exit ERR	

examples=(
    hdfs
    input_format
    pydoop_script
    self_contained
    ipcount
    sequence_file
    wordcount
    simulator
)

for e in ${examples[@]}; do
    pushd ${e}
    echo -ne "\n\n *** RUNNING ${e} EXAMPLE(S) ***\n\n"
    ./run
    popd
done
